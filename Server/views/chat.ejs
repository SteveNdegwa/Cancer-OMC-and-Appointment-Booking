<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    
    <link rel="apple-touch-icon" sizes="180x180" href="/icons/apple-touch-icon.png">
    <link rel="icon" type="image/png" sizes="32x32" href="/icons/favicon-32x32.png">
    <link rel="icon" type="image/png" sizes="16x16" href="/icons/favicon-16x16.png">
    <link rel="manifest" href="/icons/site.webmanifest">

    <link rel="stylesheet" href="/chat.css">
    <title>Chat</title>
</head>

<body>

    <form action="/chats/chat" method="post">
        <div class="navbar">
            <a class="name" href="/chats/view-profile" target="_blank" ><%= name %></a>
            <% if (consultationType == "paid") { %>
                <h3 class="expiry">Session Expiry Time : <%= exp %></h3>
            <% } %>
        </div>
        <div class="messages-container" id="messages-container">
        </div>
        <% if (sendMessageDisplay) { %>
            <div class="message-div">
                <textarea name="" id="message" class="text" cols="40" rows="5"></textarea>
                <button type="button" class="btn" id="message-btn">Message</button>
            </div>
        <% } %>
        <% if (consultationType == "paid") { %>
            <% if (accountType == "doctor") { %>
                <button type="submit" class="btn" id="renew-btn">Renew Session</button>
            <% } %>
        <% } %>
    </form>

    <script>

    </script>
    <script src="/socket.io/socket.io.js"></script>
    <script src="/chat.js"></script>
</body>

</html>